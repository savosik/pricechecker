# 📖 PriceChecker — Руководство пользователя

## Оглавление

1. [Что делает система](#1-что-делает-система)
2. [Как всё устроено (простыми словами)](#2-как-всё-устроено)
3. [Админ-панель — основная работа](#3-админ-панель)
4. [Как работает парсинг цен](#4-как-работает-парсинг-цен)
5. [VNC — виртуальный рабочий стол](#5-vnc--виртуальный-рабочий-стол)
6. [Chrome-расширение — парсер](#6-chrome-расширение)
7. [Расписание парсинга](#7-расписание-парсинга)
8. [Как проверить что всё работает](#8-как-проверить-что-всё-работает)
9. [phpMyAdmin — работа с базой данных](#9-phpmyadmin)
10. [Устранение неполадок](#10-устранение-неполадок)
11. [Доступы и пароли](#11-доступы-и-пароли)

---

## 1. Что делает система

**PriceChecker** — это сервис мониторинга цен на маркетплейсах. Он автоматически:

- Заходит на страницы товаров на **Ozon** и **Wildberries**
- Считывает текущие цены (со скидкой и без)
- Определяет продавца товара
- Сохраняет историю цен
- Отправляет уведомления при изменении цен (если настроены условия)

Система работает полностью автоматически — достаточно добавить ссылку на товар, и она начнёт отслеживать цену.

---

## 2. Как всё устроено

Система состоит из нескольких частей, каждая отвечает за своё:

| Компонент | Что делает |
|-----------|-----------|
| **Сайт (админ-панель)** | Управление товарами, ссылками, просмотр истории цен |
| **Виртуальный браузер (VNC)** | Chrome с расширением, который ходит по страницам маркетплейсов |
| **Обработчик задач (Worker)** | Извлекает цены из HTML-кода страниц |
| **База данных** | Хранит товары, ссылки, историю цен |
| **Планировщик** | Автоматически запускает парсинг по расписанию |

### Схема работы

```
Админ-панель → Создает задачу "парсить цену товара X"
       ↓
Виртуальный браузер → Открывает страницу Ozon/WB, копирует HTML
       ↓
Обработчик → Находит цену в HTML, сохраняет в историю
       ↓
Админ-панель → Показывает историю цен
```

---

## 3. Админ-панель

### Как зайти

Откройте в браузере:

```
https://pricechecker.pecado.ru/admin
```

**Логин:** `slava.triput@gmail.com`
**Пароль:** `slava999`

### Что можно делать

- **Товары** — список отслеживаемых товаров
- **Ссылки на маркетплейсы** — привязка URL-адресов Ozon/WB к товарам
- **История цен** — таблица всех зафиксированных цен с датами
- **Продавцы** — автоматически определённые продавцы
- **Маркетплейсы** — Ozon, Wildberries
- **DOM-задачи** — очередь задач на парсинг (для отладки)

### Как добавить товар для отслеживания

1. Зайдите в раздел **«Товары»**
2. Убедитесь, что нужный товар существует (товары импортируются из каталога)
3. Перейдите в раздел **«Ссылки на маркетплейсы»**
4. Нажмите **«Создать»**
5. Выберите **товар**, **маркетплейс** (Ozon или Wildberries)
6. Вставьте **URL-адрес** товара с маркетплейса (полная ссылка)
7. Поставьте галочку **«Активна»**
8. Сохраните

После этого система начнёт автоматически отслеживать цену по расписанию.

### Как посмотреть историю цен

1. Зайдите в раздел **«История цен»**
2. Вы увидите таблицу с колонками:
   - **Товар** — название
   - **Цена покупателя** — цена со скидкой (то, что видит покупатель)
   - **Базовая цена** — цена без скидки (зачёркнутая цена)
   - **Продавец** — кто продает
   - **Дата** — когда была зафиксирована цена

---

## 4. Как работает парсинг цен

### Шаг 1: Создание задачи

По расписанию (или вручную) система создаёт **DOM-задачу** — это запрос: «зайди на эту ссылку и скопируй HTML-код страницы».

### Шаг 2: Браузер выполняет задачу

В виртуальном браузере (VNC) работает **Chrome с расширением**. Расширение:

1. Каждые 5 секунд спрашивает у сервера: «есть новые задачи?»
2. Получает задачу (URL страницы на Ozon/WB)
3. Открывает новую вкладку с этой страницей
4. Ждёт загрузки страницы
5. Если это Ozon — автоматически нажимает кнопку подтверждения возраста (18+)
6. Копирует весь HTML-код страницы
7. Отправляет HTML обратно на сервер
8. Закрывает вкладку

### Шаг 3: Извлечение цены

Сервер получает HTML и извлекает из него:

- **Цену со скидкой** (цена покупателя с Ozon Картой/Банком)
- **Базовую цену** (без скидки)
- **Информацию о продавце** (название, ID)

### Шаг 4: Сохранение

Если цена изменилась по сравнению с предыдущей проверкой — создаётся новая запись в истории цен. Если цена та же — обновляется только время проверки.

---

## 5. VNC — виртуальный рабочий стол

### Что такое VNC

**VNC** (Virtual Network Computing) — это технология, которая позволяет видеть и управлять рабочим столом компьютера через интернет. В нашем случае это рабочий стол **виртуального Linux-компьютера**, на котором запущен Chrome.

Это как удалённый рабочий стол (Remote Desktop) в Windows, только через браузер.

### Зачем нужен VNC

VNC нужен для того, чтобы:

- Видеть, что делает Chrome-расширение в реальном времени
- Проверять, что расширение работает
- Отлаживать проблемы (если расширение зависло, страница не загрузилась и т.д.)
- Вручную установить или обновить Chrome

### Как подключиться к VNC

**Через браузер (noVNC — самый простой способ):**

Откройте в браузере:

```
http://194.67.127.166:6901
```

Введите пароль: **`secret`**

Вы увидите рабочий стол Linux (XFCE) с иконками на рабочем столе.

**Через VNC-клиент** (TightVNC, RealVNC и т.д.):

```
Адрес: 194.67.127.166:5901
Пароль: secret
```

### Что вы увидите на рабочем столе VNC

На рабочем столе обычно есть:

- **Chrome Parser** — ярлык Chrome-браузера с загруженным расширением для парсинга
- **Google Chrome** — обычный Chrome для ручной работы
- **Extension** — папка с файлами расширения

### Как открыть DevTools расширения (для проверки)

1. Откройте Chrome Parser
2. Введите в адресной строке: `chrome://extensions`
3. Найдите расширение **«MarketPlace Parser»**
4. Нажмите **«Сервисный работник»** (service worker) — откроется консоль DevTools
5. В консоли вы увидите логи расширения

---

## 6. Chrome-расширение

### Что оно делает

Chrome-расширение — это программа внутри браузера, которая:

- Автоматически опрашивает сервер на наличие новых задач
- Открывает страницы маркетплейсов
- Извлекает HTML-код страницы
- Отправляет результат на сервер

### Как проверить что расширение работает

#### Способ 1: Через DevTools (подробный)

1. Подключитесь к VNC (см. раздел выше)
2. Откройте Chrome → `chrome://extensions`
3. Нажмите на «Сервисный работник» у расширения MarketPlace Parser
4. В консоли должны быть сообщения:
   - `Starting worker...` — расширение запустилось
   - `No tasks available` — нет задач (нормально, если не запланирован парсинг)
   - `Processing task: 123 https://ozon.ru/...` — обрабатывается задача
   - `Task completed: 123` — задача выполнена
5. Если видите ошибки (`Failed to fetch task: 500`) — значит есть проблема

#### Способ 2: Через попап расширения (быстрый)

1. В Chrome нажмите на иконку расширения (значок пазла в правом верхнем углу)
2. Нажмите на **MarketPlace Parser**
3. Откроется маленькое окно с информацией:
   - **Статус** — Running (работает) / Stopped (остановлено)
   - **Задач выполнено** — сколько задач обработано
   - **Последняя активность** — когда последний раз была активность

#### Способ 3: Через админ-панель

1. Зайдите в админ-панель → **DOM-задачи**
2. Посмотрите статусы последних задач:
   - `pending` — ожидает обработки
   - `processing` — расширение взяло задачу, обрабатывает
   - `completed` — готово, HTML получен
   - `failed` — ошибка

---

## 7. Расписание парсинга

Парсинг цен запускается автоматически **два раза в сутки**:

| Время | Что происходит |
|-------|----------------|
| **00:00** (полночь) | Запускается команда `parse:prices` — создаются задачи на парсинг всех активных ссылок |
| **12:00** (полдень) | Повторный запуск `parse:prices` |

### Как это работает

1. В 00:00 (или 12:00) планировщик запускает команду `parse:prices`
2. Команда находит все активные ссылки на маркетплейсы
3. Для каждой ссылки создаётся задача (DOM-задача) в очереди
4. Chrome-расширение подхватывает задачи по очереди (каждые 5 секунд)
5. Результаты обрабатываются и сохраняются в историю цен

### Сколько времени занимает

- Одна задача (один товар): ~10-15 секунд
- 100 товаров: примерно 25-30 минут
- Задачи обрабатываются **последовательно** (по одной)

---

## 8. Как проверить что всё работает

### Быстрая проверка (5 минут)

1. **Откройте админ-панель** → `https://pricechecker.pecado.ru/admin`
2. **Проверьте историю цен** — есть ли свежие записи за сегодня
3. **Проверьте DOM-задачи** — все ли задачи в статусе `completed`

### Детальная проверка

#### Проверка VNC (виртуальный браузер)

1. Откройте `http://194.67.127.166:6901` (пароль: `secret`)
2. Убедитесь, что Chrome открыт и расширение активно
3. В DevTools расширения не должно быть ошибок красного цвета

#### Проверка расписания

Зайдите на сервер по SSH и выполните:

```bash
ssh root@194.67.127.166
docker exec mp_parse2_app php artisan schedule:list
```

Вы увидите список запланированных задач и время следующего запуска.

#### Ручной запуск парсинга (тест)

Если хотите запустить парсинг прямо сейчас (не дожидаясь расписания):

```bash
ssh root@194.67.127.166
docker exec mp_parse2_app php artisan parse:prices
```

Это создаст задачи на парсинг всех активных ссылок. Chrome-расширение начнёт их обрабатывать автоматически.

#### Проверка отдельного товара

Чтобы проверить парсинг одного конкретного товара:

```bash
ssh root@194.67.127.166
docker exec mp_parse2_app php artisan dom:dispatch --marketplace=ozon --limit=1
```

Через 10-15 секунд задача должна перейти в статус `completed`, а в истории цен появится новая запись.

---

## 9. phpMyAdmin

### Что это

**phpMyAdmin** — это веб-интерфейс для работы с базой данных MySQL. Здесь хранятся все данные: товары, ссылки, цены, задачи.

### Как зайти

```
http://194.67.127.166:8081
```

Вход автоматический (логин и пароль настроены).

### Полезные таблицы

| Таблица | Содержимое |
|---------|-----------|
| `products` | Товары |
| `product_links` | Ссылки на маркетплейсы |
| `price_histories` | История цен |
| `dom_tasks` | Задачи на парсинг |
| `sellers` | Продавцы |
| `marketplaces` | Маркетплейсы (Ozon, WB) |

### Что можно посмотреть

- **Последние цены**: откройте таблицу `price_histories`, отсортируйте по `created_at` по убыванию
- **Статусы задач**: откройте `dom_tasks`, посмотрите поле `status`
- **Активные ссылки**: откройте `product_links`, посмотрите поле `is_active`

---

## 10. Устранение неполадок

### Проблема: Цены не обновляются

**Проверьте:**

1. **VNC работает?** — откройте `http://194.67.127.166:6901`
2. **Chrome и расширение запущены?** — на рабочем столе VNC должен быть открыт Chrome
3. **Расширение активно?** — в `chrome://extensions` расширение должно быть включено
4. **Есть задачи?** — в админ-панели проверьте DOM-задачи

**Решение:**

- Перезапустите браузер: `docker restart mp_parse2_browser`
- Подождите 2-3 минуты, пока Chrome запустится и расширение подключится

### Проблема: Задачи зависают в статусе «processing»

Задача была взята, но не завершена (например, страница не загрузилась).

**Решение:**

```bash
ssh root@194.67.127.166
# Сбросить зависшие задачи (старше 10 минут)
docker exec mp_parse2_app php artisan tinker --execute="
DB::table('dom_tasks')
  ->where('status', 'processing')
  ->where('started_at', '<', now()->subMinutes(10))
  ->update(['status' => 'pending', 'worker_id' => null, 'started_at' => null]);
echo 'Done';
"
```

### Проблема: VNC не открывается

**Проверьте:**

1. Сервер доступен: `ping 194.67.127.166`
2. Контейнер запущен: `docker ps | grep browser`
3. Если не запущен: `docker start mp_parse2_browser`

### Проблема: Расширение показывает ошибку 401

Неправильный API-токен. Chrome-расширение отправляет токен, который не совпадает с серверным.

**Решение:**

1. Проверьте токен на сервере: `grep DOM_PARSER_API_TOKEN /var/www/mp_parse2/.env`
2. Проверьте токен в расширении: посмотрите файл `config.json` в папке расширения
3. Токены должны совпадать

### Проблема: Расширение показывает ошибку 500

Ошибка на сервере. Обычно связана с правами на лог-файлы после перезапуска контейнера.

**Решение:**

```bash
ssh root@194.67.127.166
docker exec mp_parse2_app bash -c 'chown -R www-data:www-data /var/www/html/storage/logs/'
docker restart mp_parse2_app
```

---

## 11. Доступы и пароли

### Веб-интерфейсы

| Сервис | URL | Логин | Пароль |
|--------|-----|-------|--------|
| Админ-панель | `https://pricechecker.pecado.ru/admin` | `admin@example.com` | `admin123` |
| VNC (через браузер) | `http://194.67.127.166:6901` | — | `secret` |
| phpMyAdmin | `http://194.67.127.166:8081` | автоматический | автоматический |

### VNC-клиент

| Параметр | Значение |
|----------|----------|
| Адрес | `194.67.127.166` |
| Порт VNC | `5901` |
| Порт noVNC (веб) | `6901` |
| Пароль | `secret` |

### SSH (сервер)

| Параметр | Значение |
|----------|----------|
| Адрес | `194.67.127.166` |
| Пользователь | `root` |
| Путь к проекту | `/var/www/mp_parse2` |

### База данных

| Параметр | Значение |
|----------|----------|
| Хост | `mysql` (внутри Docker) |
| Порт | `3306` (внутри) / `3307` (снаружи) |
| База | `laravel` |
| Пользователь | `laravel` |
| Пароль | `root` |

---

> **Совет:** Если что-то не работает — сначала перезапустите контейнеры:
> ```bash
> ssh root@194.67.127.166
> cd /var/www/mp_parse2
> docker compose restart
> ```
> Подождите 2-3 минуты, и проверьте снова.
